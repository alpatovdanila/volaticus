Inventory is a directory that manages and stores all the assets that would be available in the game

There is the following types of resources
effects
materials
models
sfx

Each of them has its own format and its own generators